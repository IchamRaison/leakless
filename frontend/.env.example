# Frontend: no secrets here. Telegram alert credentials live on the backend only
# (repository-root .env, read by the API server): TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID,
# optional ALERT_PERSISTENCE_SECONDS (default 30).
